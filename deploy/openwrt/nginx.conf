# 可选：用 nginx 反向代理 LinAI，避免直接暴露 3000 端口。
# OpenWrt 装包：opkg update && opkg install nginx
# 放到 /etc/nginx/conf.d/linai.conf（或按你的 nginx 版本 include 进 http{}）。
#
# 注意：不同 GL.iNet 固件可能让 LuCI 占用 80 或 8080。
# 部署前先用 ss -lntp（或 netstat -lntp）检查端口；这里默认用 8081。

server {
    listen 8081;
    server_name _;

    # 参考图上传可能较大，放宽 body 限制
    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 高画质图片可能需要数分钟。默认 60s 会在任务完成前断开浏览器到
        # LinAI 的请求；同时关闭缓冲，确保 SSE 任务更新能及时到达前端。
        proxy_read_timeout 900s;
        proxy_send_timeout 900s;
        proxy_buffering off;
    }
}
